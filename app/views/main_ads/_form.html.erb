<%= form_for @main_ad, :html => { :multipart => true } do |f| %>
  <% if @main_ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@main_ad.errors.count, "error") %> prohibited this main_ad from being saved:</h2>

      <ul>
      <% @main_ad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
