<%= form_for @advert2, :html => { :multipart => true } do |f| %>
  <% if @advert2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@advert2.errors.count, "error") %> prohibited this advert2 from being saved:</h2>

      <ul>
      <% @advert2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
