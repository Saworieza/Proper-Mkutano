<%= form_for @advert1, :html => { :multipart => true } do |f| %>
  <% if @advert1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@advert1.errors.count, "error") %> prohibited this advert1 from being saved:</h2>

      <ul>
      <% @advert1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
